apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visual-regression-tracker.fullname" . }}-test-connection-api"
  labels:
    {{- include "visual-regression-tracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visual-regression-tracker.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
